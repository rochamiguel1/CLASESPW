clase ->molde de lo que luego seran los objetos
		el punto inicial para poder ejecutar codigos (metodos)
		El nombre de las clases en Mayuscula la primera
		El nombre en singular
		El nombre, representativo
		El nombre no puede ser una palabra reservada

metodos -> donde se definen las fuciones
			dentro de estos se pueden llamar a ejecuciones
			El nombre, representativo
			El nombre, la primera en minusculas
			mod_acceso retorno nombre (argumetos){ cuerpo }

			main -> lo que se ejecuta cuando le doy al play

			System.out.println("Frase") -> imprime por consola (mete un enter)
			System.out.print("Frase") -> imprime por consola (no mete un salto de linea)
			"\n"
variables -> un espacio donde voy a guardar un dato para poder reutilizarlo
			El nombre de las variables en minusculas (salvo casos especiales)
			El nombre de las variables representativo
			El nombre sin caracteres especiales 
			El nombre empieza con minusculaMayuscula
			Las variables se definen dentro de una clase
			Las variables se utilizan dentro de un metodo
			Las variables tienen nombres unicos en el ambito. No se puede crear dos variables con el mismo nombre
			Si le quiero cambiar el valor a una variable, simplemente reasigna valor con el =

			"modificador acceso" tipo nombre = valor

				String -> palabras que van siempre entre comilla doble "palabra"
				char -> guarda una letra entre comilla simple 'B'. 11111111 -> 1byte 8bits
				int -> numeros enteros (sin decimales)
				float -> numeros con comas (siempre termina con la letra f)
				double -> numeros con comas (el doble de precision en la coma que un float)
				boolean -> true / false. Utilizado para realizar comprobaciones condicionales 

				tipo[] -> Array de elementos 
				Object -> cualquier cosa

			Clasificacion segun su tipo: String, int, double, float, boolean, char
			Clasificacion segun su forma de construirse: 
				primitivas: solo guarda el dato correspondiente
				complejas: ademas del dato, guarda la funcionalidad extra que se puede utilizar. Porque es un tipo asociado a una clase
			Clasificacion segun su forma de guardarse: 
				constante o final -> aquella que no puede cambiar nunca. Se le añade el modificador final
					Este tipo de variables tiene el nombre en MAYUSCULAS
				mutable -> aquellas que en cualquier momento pueden cambiar de valor

			Clasificacion segun su ambito: donde tiene vida la variable
				clase - global
				bloque - local
